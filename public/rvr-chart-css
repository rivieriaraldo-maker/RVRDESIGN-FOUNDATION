/*
 * RVR Chart System — CSS (RVRDESIGN)
 * RVRDESIGN FOUNDATION · Etapa 06 (aberta por RVRDESIGN-015)
 *
 * Infraestrutura visual dos 7 componentes oficiais do Chart System.
 * Consome exclusivamente variáveis de rvr-tokens.css. Nenhuma cor,
 * espaçamento ou fonte declarada aqui fora dos tokens.
 *
 * Ordem de carregamento: rvr-tokens.css → rvr-typography.css →
 * rvr-motion.css → rvr-components.css → rvr-chart.css
 * Path sugerido: /public/rvr-chart.css
 */

/* ============ RVRChartContainer ============ */
.rvr-chart-container {
  position: relative;
  background-color: var(--rvr-bg-secondary);
  border: 1px solid var(--rvr-border);
  border-radius: var(--rvr-radius-lg);
  box-shadow: var(--rvr-shadow-sm);
  padding: var(--rvr-space-lg);
  width: 100%;
  box-sizing: border-box;
}
.rvr-chart-container--glass {
  background-color: rgba(255,255,255,0.04);
  border-color: rgba(255,255,255,0.14);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
}

/* ============ RVRChartTitle ============ */
.rvr-chart-title { margin-bottom: var(--rvr-space-md); }
.rvr-chart-title__main {
  font-family: var(--rvr-font-interface);
  font-size: var(--rvr-fs-h4);
  line-height: var(--rvr-lh-h4);
  font-weight: var(--rvr-fw-semibold);
  color: var(--rvr-text-primary);
}
.rvr-chart-title__subtitle {
  font-family: var(--rvr-font-interface);
  font-size: var(--rvr-fs-body-sm);
  line-height: var(--rvr-lh-body-sm);
  color: var(--rvr-text-secondary);
  margin-top: 2px;
}

/* ============ RVRLegend ============ */
.rvr-legend { display: flex; flex-wrap: wrap; gap: var(--rvr-space-md); margin-top: var(--rvr-space-md); }
.rvr-legend__item {
  display: inline-flex;
  align-items: center;
  gap: var(--rvr-space-xs);
  font-family: var(--rvr-font-interface);
  font-size: var(--rvr-fs-caption);
  color: var(--rvr-text-secondary);
}
.rvr-legend__swatch { width: 10px; height: 10px; border-radius: var(--rvr-radius-full); flex-shrink: 0; }

/* ============ RVRTooltip ============ */
.rvr-chart-tooltip {
  position: absolute;
  pointer-events: none;
  background-color: rgba(20,20,20,0.9);
  border: 1px solid var(--rvr-border);
  border-radius: var(--rvr-radius-sm);
  box-shadow: var(--rvr-shadow-md);
  padding: var(--rvr-space-xs) var(--rvr-space-sm);
  font-family: var(--rvr-font-data);
  font-size: var(--rvr-fs-caption);
  color: var(--rvr-text-primary);
  transition-property: opacity, transform;
  transition-duration: var(--rvr-transition-fast);
  transition-timing-function: var(--rvr-easing);
  z-index: 10;
}
.rvr-chart-tooltip--glass { background-color: rgba(255,255,255,0.06); backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); }
.rvr-chart-tooltip--hidden { opacity: 0; transform: translateY(4px); }

/* ============ RVRAxis ============ */
.rvr-chart-axis {
  display: flex;
  font-family: var(--rvr-font-data);
  font-size: var(--rvr-fs-caption);
  color: var(--rvr-text-secondary);
}
.rvr-chart-axis--x { flex-direction: row; justify-content: space-between; border-top: 1px solid var(--rvr-border); padding-top: var(--rvr-space-xs); }
.rvr-chart-axis--y { flex-direction: column-reverse; justify-content: space-between; border-right: 1px solid var(--rvr-border); padding-right: var(--rvr-space-xs); }

/* ============ RVRGridLines ============ */
.rvr-chart-gridlines {
  position: absolute;
  inset: 0;
  background-image: repeating-linear-gradient(
    to bottom,
    var(--rvr-border) 0,
    var(--rvr-border) 1px,
    transparent 1px,
    transparent calc(100% / 4)
  );
  pointer-events: none;
}

/* ============ RVRSeries ============ */
.rvr-chart-series { position: relative; }
